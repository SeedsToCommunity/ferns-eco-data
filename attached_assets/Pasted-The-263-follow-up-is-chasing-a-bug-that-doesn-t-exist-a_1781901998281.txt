The #263 follow-up is chasing a bug that doesn't exist — and "fixing" it would corrupt good data. The audit asserts Daucus carota's seed_group_names should contain "Do Not Collect." The live data has ["Hitchhikers"], and that's correct. "Do Not Collect" is not a seed group at all — it's a phrase buried in the boilerplate description of the Hitchhikers dispersal group (same sentence repeated across 22 records). So the assertion conflated a dispersal category with a collection-status flag. The right fix is to correct or drop the assertion (assert Hitchhikers, or test the flag wherever it actually lives), not touch the IDP. If someone works #263 as written and jams "Do Not Collect" into seed_group_names, they'll poison a legitimate phenology field. Reframe #263 as an assertion bug before anyone acts on it.
Both stored Field Museum URLs are broken, in different ways. The real working form is https://fieldguides.fieldmuseum.org/guides/guide/{id}. The registry's website_url_patterns.guide drops the /guide/ segment (→ 404), and field_museum_url in guides-data uses a different host entirely (science.fieldmuseum.org/fieldguides/...). They also disagree with each other. Since human-navigable source URLs are the whole point of the provenance envelope, a dead pattern quietly defeats it. Both should be the fieldguides.fieldmuseum.org/guides/guide/{id} form.
The description fix isn't in the file you handed me as final. The summary says it changed "494 native plant species" → "494 native and introduced plant species" in metadata.ts, but LCSCG_REGISTRY_ENTRY.description (line 56) still reads "494 native plant species." Either this is a stale copy or the edit hit the wrong field — worth confirming the change actually landed.
Two minor: the route file's header comment calls the source "Lake County Savanna and Grassland Guide" (wrong — it's Seed Collection Guides), and the adapter passes sourceKind: "in-memory" as a string, which is worth a glance against the current envelope builder since that field's been a boolean elsewhere — lower confidence, the contract v1 in docs doesn't even define sourceKind.